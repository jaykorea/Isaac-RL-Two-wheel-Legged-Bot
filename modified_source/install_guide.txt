
# Set the ISAACSIM_PATH environment variable to point to your Orbit installation directory
export ISAACSIM_PATH="${HOME}/.local/share/ov/pkg/isaac-sim-4.0.0"
export ISAACSIM_PYTHON_EXE="${ISAACSIM_PATH}/python.sh"
# replace 'source' folder into your isaaclab 'source' folder


# ${ISAACLAB_PATH}/orbit.sh -p -m pip install --upgrade pip
# ${ISAACLAB_PATH}/orbit.sh -p -m pip install -e .
